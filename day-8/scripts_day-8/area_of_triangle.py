def triangle_area(base, height):
    
    area = 0.5 * base * height
    return area

# Example usage
base_length = 10
height_length = 5

area = triangle_area(base_length, height_length)
print(f"The area of the triangle with base {base_length} and height {height_length} is {area}.")
